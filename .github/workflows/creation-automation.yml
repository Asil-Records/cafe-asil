name: Café Asil Content Automation

on:
  workflow_dispatch:
    inputs:
      playlist-name:
        description: 'Choose playlist type'
        required: true
        default: 'lofi-moments'
        type: choice
        options:
          - lofi-moments
          - sleepy-cafe
          - morning-brew

jobs:
    content_creation:
        runs-on: ubuntu-latest
        container:
            image: ghcr.io/asil-records/create-session-image:v1
        
        steps:
            - name: Checkout repo
              uses: actions/checkout@v4

            - name: Write rclone config
              run: |
                mkdir -p ~/.config/rclone
                cat > ~/.config/rclone/rclone.conf <<EOF
                ${{ secrets.RCLONE_CONFIG }}
                EOF
                echo "Config file written."

            - name: Prepare variables
              id: vars
              run: |
                PLAYLIST_NAME="${{ github.event.inputs.playlist-name }}"
                SESSION_DATE=$(date +%Y%m%d)
                REMOTE_BASE="cafe-asil:Asil-Records/cafe-asil"
                REMOTE_SESSION_BASE="${REMOTE_BASE}/sessions/session_${SESSION_DATE}_${PLAYLIST_NAME}"

                FINAL_REMOTE="$REMOTE_SESSION_BASE"
                COUNTER=1

                # Check if session folder already exists, and if so, increment suffix
                while rclone lsf "$FINAL_REMOTE" >/dev/null 2>&1; do
                    FINAL_REMOTE="${REMOTE_SESSION_BASE}_${COUNTER}"
                    COUNTER=$((COUNTER+1))
                done

                echo "SESSION_DIR=$FINAL_REMOTE" >> $GITHUB_ENV

            - name: Create session folders
              run: |
                rclone mkdir "$SESSION_DIR/assets/audio"
                rclone mkdir "$SESSION_DIR/assets/video"
                rclone mkdir "$SESSION_DIR/merged"
                echo "Session directory created: $SESSION_DIR"
                echo "- Successfully created session folders: $SESSION_DIR"

