name: Café Asil Content Automation

on:
  workflow_dispatch:
    inputs:
      playlist-name:
        description: 'Choose playlist type'
        required: true
        default: 'lofi-moments'
        type: choice
        options:
          - lofi-moments
          - sleepy-cafe
          - morning-brew
      requested-count:
        description: 'Total count of songs to create content'
        required: true
        default: '3'
        type: 'string'

jobs:
    content_creation:
        runs-on: ubuntu-latest
        container:
            image: ghcr.io/asil-records/create-session-image:v2
        
        steps:
            - name: Checkout repo
              uses: actions/checkout@v4

            - name: Write rclone config
              run: |
                mkdir -p ~/.config/rclone
                cat > ~/.config/rclone/rclone.conf <<EOF
                ${{ secrets.RCLONE_CONFIG }}
                EOF
                echo "Config file written."

            - name: Prepare variables
              id: vars
              run: |
                PLAYLIST_NAME="${{ github.event.inputs.playlist-name }}"
                REQUESTED_COUNT="${{ github.event.inputs.requested-count }}"
                SESSION_DATE=$(date +%Y%m%d)
                REMOTE_BASE="cafe-asil:Asil-Records/cafe-asil"
                REMOTE_RAW_AUDIO="${REMOTE_BASE}/raw_audios/${PLAYLIST_NAME}"
                REMOTE_SESSION_BASE="${REMOTE_BASE}/sessions"
                REMOTE_SESSION_BASE_DIR="${REMOTE_SESSION_BASE}/session_${SESSION_DATE}_${PLAYLIST_NAME}"
                REMOTE_SESSION_AUDIO_DIR="${SESSION_DIR}/assets/audios"
                REMOTE_SESSION_IMAGE_DIR="${SESSION_DIR}/assets/images"
                REMOTE_SESSION_MERGED_DIR="${SESSION_DIR}/merged"

                echo "PLAYLIST_NAME=$PLAYLIST_NAME" >> $GITHUB_ENV
                echo "REQUESTED_COUNT=$REQUESTED_COUNT" >> $GITHUB_ENV
                echo "REMOTE_RAW_AUDIO=$REMOTE_RAW_AUDIO" >> $GITHUB_ENV
                echo "REMOTE_SESSION_BASE=$REMOTE_SESSION_BASE" >> $GITHUB_ENV
                echo "REMOTE_SESSION_BASE_DIR=$REMOTE_SESSION_BASE_DIR" >> $GITHUB_ENV
                echo "REMOTE_SESSION_AUDIO_DIR=$REMOTE_SESSION_AUDIO_DIR" >> $GITHUB_ENV
                echo "REMOTE_SESSION_IMAGE_DIR=$REMOTE_SESSION_IMAGE_DIR" >> $GITHUB_ENV
                echo "REMOTE_SESSION_MERGED_DIR=$REMOTE_SESSION_MERGED_DIR" >> $GITHUB_ENV

                echo "🔹 Variables prepared"

            - name: Update script permissions
              run: chmod +x ./scripts/*.sh

            - name: Update session path
              shell: bash
              run: |
                  ./scripts/update_session_path.sh "${REMOTE_SESSION_BASE_DIR}" "${REMOTE_SESSION_BASE}"

            - name: Create session folders
              run: |
                echo "🔹 Creating session folders..."
                rclone mkdir "${REMOTE_SESSION_MERGED_DIR}"
                rclone mkdir "${REMOTE_SESSION_AUDIO_DIR}"
                rclone mkdir "${REMOTE_SESSION_IMAGE_DIR}"

            - name: Pull requested audio files
              shell: bash
              run: |
                ./scripts/pull_raw_audios.sh "${PLAYLIST_NAME}" "${REQUESTED_COUNT}" "${SESSION_DIR}"

            - name: Download all session audio files
              run: |
                mkdir -p ./session_audio
                echo "Downloading MP3s from ${REMOTE_SESSION_AUDIO_DIR}..."
                rclone --transfers=5 copy "${REMOTE_SESSION_AUDIO_DIR}" ./session_audio --include "*.mp3" --progress

            

            
              


